# ピクミンブルーム 素材収集トラッカー - CLAUDE.md

## 概要・コンセプト

ピクミンブルームの毎月開催されるイベントで集める素材（例：ピクセルアートフラワー）の収集進捗を管理するWebアプリ。
ゲーム画面に表示されている「累計目標個数」と「あと○○個」の2つを入力するだけで、月末コンプリートまでに今のペースで間に合うかを確認できる。

**解決したい課題：**
- 今のペースで月末までにコンプリートできるか不安
- 毎日どれくらい集めればよいかわからない
- 週末の巨大キノコを加味した現実的なペースを把握したい

## ターゲットユーザー

- ピクミンブルームを毎月プレイしているユーザー
- 毎月のイベントでMiiコスチューム・デコピクミンのコンプリートを目指している人
- スマホでピクミンブルームをプレイしながら、ブラウザで進捗をサッと確認したい人

## 主要機能一覧

### 必須機能

| 優先度 | 機能 | 説明 |
|--------|------|------|
| 高 | 入力フォーム | 「確認日」「累計目標」「あと」の3項目を入力 |
| 高 | 現在の累計表示 | 累計目標 − あと○○個 = 現在の累計を自動計算 |
| 高 | 残り個数表示 | コンプリート目標 − 現在の累計 = コンプリートまでの残り個数 |
| 高 | 残り日数表示 | 確認日から月末までの日数（確認日を含む） |
| 高 | 必要ペース表示 | 残り個数 ÷ 残り日数 = 1日あたり必要個数 |
| 高 | 現在ペース表示 | 現在の累計 ÷ イベント経過日数（月初1日起算） |
| 高 | 間に合うか判定 | 現在ペース ≥ 必要ペース → ✅ 間に合う / ❌ 遅れている |
| 高 | 折れ線グラフ | 目標ペース・現在地・現在ペースの3本を表示 |
| 高 | 設定画面 | 毎月イベント開始時に設定する項目を管理 |
| 高 | データ保存 | 入力値・設定値をLocalStorageに保存（再訪時に復元） |
| 高 | 月変わり自動リセット | 月が変わったら入力値・巨大キノコ開催日を自動でリセット |
| 高 | PWA対応 | ホーム画面に追加してアプリのように起動できる |

### 設定画面の項目

月初のイベント開始時に一度だけ設定する。設定値はLocalStorageに保存され次回以降も保持される。

| 設定項目 | 入力方式 | デフォルト値 | 説明 |
|----------|----------|--------------|------|
| コンプリート目標個数 | 数値入力 | 12,000 | 毎月公式サイトで確認して入力 |
| 巨大キノコ開催日 | カレンダー形式のチェックボックス | なし | 公式サイトのスケジュールを見て該当日にチェック（任意） |
| 公式サイトリンク | ボタン | 固定URL | タップすると公式ニュースページを開く |

**巨大キノコ開催日について：**
- 設定は任意。未設定の場合は理想ライン（目標ペース）が均等な直線になる
- 設定した場合：該当日に600個加算した傾斜ラインを描画
- 月が変わると自動リセットされるため、毎月設定が必要

### グラフ仕様

- **目標ペース（青・実線）**：巨大キノコ開催日を加味した理想の累計ライン
  - 平日：（コンプリート目標 − キノコボーナス合計） ÷ 平日日数 を均等配分
  - 巨大キノコ開催日：3回 × 200個 = 600個/日 を加算
  - 巨大キノコ未設定時：均等な直線
- **現在地（★ オレンジ）**：確認日時点の累計をプロット
- **現在ペース（破線・赤）**：現在のペースが続いた場合の月末予測

### 判定バッジのパターン

| 表示 | 条件 |
|------|------|
| 🎉 コンプリート達成！おめでとうございます！ | 残り個数 ≤ 0 |
| ❌ イベント期間が終了しました | 残り日数 ≤ 0（月末を過ぎた日付を確認日に設定した場合） |
| ✅ 今のペースで間に合います！ ＋ ○月○日コンプペース | 現在ペース ≥ 必要ペース |
| ❌ ペースが遅れています | 現在ペース ＜ 必要ペース |

### 入力フィールドのバリデーション

- **確認日**：当月の日付のみ選択可。カレンダーアイコンからの操作のみ（キーボード入力禁止）。今日以外の日付を選択するとフィールド下に警告を表示
- **累計目標・あと**：累計目標 < あと の場合にフィールド下にエラーを表示

### スコープ外（やらないこと）

- ログイン・ユーザー認証
- 複数デバイス同期
- 公式サイトからの自動データ取得（CORS制限のためブラウザアプリ単体では不可）
- 日ごとの記録履歴管理
- 素材入手手段ごとの詳細入力
- 通知機能
- 巨大キノコ開催日の事前登録（ユーザーが毎月手動で入力する運用）

## 画面構成

### 画面一覧

| 画面名 | 役割 |
|--------|------|
| メイン画面 | 進捗入力・サマリー表示・グラフ表示 |
| 設定画面 | コンプリート目標・巨大キノコ開催日の設定 |

### メイン画面レイアウト

```
┌─────────────────────────────┐
│  🍄 素材収集トラッカー  ⚙️  │  ← ⚙️タップで設定画面へ
├─────────────────────────────┤
│  📥 ゲーム画面から入力      │
│  確認日（当月のみ）[5月5日] │
│  累計目標       [    600]個 │
│  あと           [    390]個 │
│                   [更新]    │
├─────────────────────────────┤
│  進捗サマリー               │
│  現在の累計：    210個      │
│  コンプリートまで：11,790個 │
│  残り日数：       27日      │
│  ─────────────────────────  │
│  必要ペース    437個/日     │
│  現在ペース    42個/日      │
│  ❌ ペースが遅れています    │
├─────────────────────────────┤
│  グラフ                     │
│  ── 目標ペース              │
│  ★  現在地                 │
│  ‥  現在ペース              │
│                         v8  │
└─────────────────────────────┘
```

### 設定画面レイアウト

```
┌─────────────────────────────┐
│  ← 設定                    │  ← 戻るボタン
├─────────────────────────────┤
│  🎯 コンプリート目標個数    │
│  [ 12,000 ]個               │
├─────────────────────────────┤
│  🍄 巨大キノコ開催日（Y年M月）│
│  公式サイトを確認してください│
│  [公式サイトを開く 🔗]      │
│  チェックした日は+600個/日  │
│  （カレンダー形式）          │
│  □ 1  □ 2  ☑ 3  ☑ 4      │
│  □ 5  □ 6  □ 7  □ 8      │
│  ...                        │
├─────────────────────────────┤
│           [保存する]        │
└─────────────────────────────┘
```

### 画面遷移

```
メイン画面 ──（⚙️タップ）──→ 設定画面
          ←─（保存 / 戻る）──
```

## 使用技術スタック

| 種別 | 技術 | 理由 |
|------|------|------|
| HTML | HTML5 | シンプル構成・追加設定不要 |
| スタイル | CSS3 | フレームワークなし・軽量 |
| ロジック | Vanilla JavaScript (ES6+) | ビルド不要・シンプル |
| グラフ | Chart.js (CDN) | CDN読み込みのみ・設定ゼロ |
| データ保存 | LocalStorage | サーバー不要・スマホ内完結 |
| PWA | Web App Manifest + Service Worker | ホーム画面追加・オフライン対応 |
| 実行環境 | スマホブラウザ（iOS Chrome / Android Chrome） | インストール不要 |

**開発・コメントはすべて日本語で行う。**

## ディレクトリ構成

```
ピクミンブルームパーツペースメーカー/
├── index.html          # メイン画面
├── settings.html       # 設定画面
├── style.css           # 共通スタイル
├── app.js              # メイン画面ロジック（計算・表示・LocalStorage）
├── settings.js         # 設定画面ロジック
├── chart-config.js     # Chart.js グラフ設定
├── storage.js          # LocalStorage の読み書き・共通ユーティリティ
├── manifest.json       # PWA マニフェスト
├── sw.js               # Service Worker（オフライン対応・キャッシュ管理）
└── icons/
    ├── icon-192.png    # PWAアイコン（192×192）
    └── icon-512.png    # PWAアイコン（512×512）
```

## LocalStorageのデータ構造

```javascript
// キー：pikmin_tracker
{
  // 設定値（月初に一度設定）
  "completeGoal": 12000,           // コンプリート目標個数
  "bigMushroomDays": [3, 4, 10, 11, 17, 18, 24, 25], // 巨大キノコ開催日（日付の配列）

  // 入力値（更新のたびに上書き）
  "nextGoal": 600,                 // 累計目標個数
  "remaining": 390,                // あと○○個
  "updatedAt": "2026-05-05",       // 最終更新日

  // 月変わり検知用
  "lastActiveMonth": "2026-05"     // 最後にアクセスした年月（YYYY-MM形式）
}
```

**月変わり時の動作：**
アプリ起動時に `lastActiveMonth` と今日の年月を比較し、異なる場合は `nextGoal`・`remaining`・`bigMushroomDays` を自動リセットする。年月は常に今日の日付から取得（設定値として保存しない）。

## Service Workerのキャッシュ管理

- キャッシュ名は `pikmin-tracker-vN`（Nはバージョン番号）
- ファイルを更新した際は `sw.js` の `CACHE_NAME` のバージョン番号を上げる
- `index.html` 末尾のバージョン表示（`v8` など）も合わせて更新する
- スマホで更新が反映されない場合はブラウザのキャッシュ・サイトデータを削除する

## セットアップ手順

```bash
# 1. ローカルサーバーを起動（PWAのService WorkerはHTTPSまたはlocalhostが必要）
cd /Applications/ピクミンブルームパーツペースメーカー
python3 -m http.server 8080

# 2. PCブラウザで開く
# http://localhost:8080

# 3. スマホで使う場合（同じWi-Fi接続が必要）
# PCのIPアドレスを確認
ipconfig getifaddr en0

# スマホのブラウザで以下を開く
# http://（上記のIPアドレス）:8080
```

## よく使うコマンド

```bash
# ローカル開発サーバー起動
python3 -m http.server 8080

# PCのIPアドレス確認（スマホからアクセスする場合）
ipconfig getifaddr en0
```

## 開発ルール・コーディング規約

- **言語**：コード・コメント・変数名はすべて日本語または日本語コメント付きで書く
- **コメント**：関数・処理ブロックごとに日本語でコメントを記載する
- **変数名**：英語の変数名を使う場合もコメントで日本語説明を添える
- **文字コード**：UTF-8
- **インデント**：スペース2つ
- **ファイル分割**：役割ごとにファイルを分ける（HTML / CSS / JS）
- **外部ライブラリ**：Chart.js のみCDNで読み込む（npm不使用）
- **ファイル更新時**：必ず `sw.js` のキャッシュバージョンと `index.html` のバージョン表示を上げる

## テスト方針

自動テストは導入しない（シンプル構成のため）。以下のケースを手動で動作確認する：

- 月初（1日）に入力した場合の計算が正しいか
- 月末（31日）に入力した場合の計算が正しいか
- 残り日数が0になった場合の表示（❌ イベント期間が終了しました）
- 目標個数を超えた場合の表示（🎉 コンプリート達成！）
- 設定画面で巨大キノコ日を変更した場合にグラフの目標ペースラインが更新されるか
- 月が変わったときにデータが自動リセットされるか
- LocalStorageへの保存・復元が正しく動くか
- PWAとしてホーム画面に追加後、オフラインで開けるか
- PC・スマホ両方で動作確認する（バージョン番号が一致しているか確認）

## 非機能要件・制約

| 項目 | 内容 |
|------|------|
| 認証 | なし（不要） |
| データ永続化 | LocalStorageのみ（スマホ内・ブラウザデータ削除でリセットされる） |
| オフライン | Service Workerでオフライン閲覧可能（初回アクセス後） |
| 対応ブラウザ | iOS Chrome / Android Chrome（最新版） |
| パフォーマンス | 外部通信なし・即時表示 |
| イベント期間 | 毎月1日〜月末（月ごとに自動計算） |
| 残り日数計算 | 確認日から月末までの日数（確認日を含む） |
| 巨大キノコボーナス | 3回 × 200個 = 600個/日（設定画面で指定した日に適用・任意設定） |
| 多言語 | 日本語のみ |
| サーバー | 不要（静的ファイルのみ） |
| 自動データ取得 | 非対応（CORSのため）。設定画面から手動入力する |

## 毎月のイベント開始時にやること

アプリ起動時に月が変わっていれば自動でリセットされる。その後：

1. 公式サイト（https://pikminbloom.com/ja/news/）で今月のイベント情報を確認する
2. アプリの設定画面（⚙️）を開く
3. **コンプリート目標個数**を確認・更新する
4. **巨大キノコ開催日**を公式スケジュールに合わせてチェックする（任意）
5. 「保存する」をタップして完了

## 素材入手手段の参考情報

| 手段 | 獲得数 | 頻度・条件 |
|------|--------|------------|
| 🌸 花植え | 花500本で約5個（約5分歩く） | 毎日・歩いた分だけ |
| 🍄 通常キノコ | 70〜100個/回（ランダム） | 1日3回無料枠（巨大キノコと共通） |
| 🍄 巨大キノコ | 200個/回 × 3回 = 600個/日 | 設定画面で指定した日のみ |
| 🎯 チャレンジリワード | 不定期 | クリア時 |

※ 通常キノコ・巨大キノコの無料枠は1日3回で共通カウント
